Submodule ggml contains modified content
diff --git src/core/util.cpp src/core/util.cpp
index ff53c65..5e09dba 100644
--- src/core/util.cpp
+++ src/core/util.cpp
@@ -594,28 +594,32 @@ std::string trim(const std::string& s) {
 static sd_log_cb_t sd_log_cb = nullptr;
 void* sd_log_cb_data         = nullptr;
 
-#define LOG_BUFFER_SIZE 4096
-
 void log_printf(sd_log_level_t level, const char* file, int line, const char* format, ...) {
     va_list args;
     va_start(args, format);
+    va_list measured_args;
+    va_copy(measured_args, args);
+    int required = vsnprintf(nullptr, 0, format, measured_args);
+    va_end(measured_args);
 
-    static char log_buffer[LOG_BUFFER_SIZE + 1];
-    int written = snprintf(log_buffer, LOG_BUFFER_SIZE, "%s:%-4d - ", sd_basename(file).c_str(), line);
-
-    if (written >= 0 && written < LOG_BUFFER_SIZE) {
-        vsnprintf(log_buffer + written, LOG_BUFFER_SIZE - written, format, args);
+    std::string message;
+    if (required >= 0) {
+        std::vector<char> buffer(static_cast<size_t>(required) + 1);
+        vsnprintf(buffer.data(), buffer.size(), format, args);
+        message.assign(buffer.data(), static_cast<size_t>(required));
+    } else {
+        message = "native logger could not format this complete message; format string: ";
+        message += format == nullptr ? "<null>" : format;
     }
-    size_t len = strlen(log_buffer);
-    if (log_buffer[len - 1] != '\n') {
-        strncat(log_buffer, "\n", LOG_BUFFER_SIZE - len);
-    }
-
-    if (sd_log_cb) {
-        sd_log_cb(level, log_buffer, sd_log_cb_data);
-    }
-
     va_end(args);
+
+    std::string output = sd_basename(file) + ":" + std::to_string(line) + " - " + message;
+    if (output.empty() || output.back() != '\n') {
+        output.push_back('\n');
+    }
+    if (sd_log_cb) {
+        sd_log_cb(level, output.c_str(), sd_log_cb_data);
+    }
 }
 
 void sd_set_log_callback(sd_log_cb_t cb, void* data) {
diff --git src/model/diffusion/krea2.hpp src/model/diffusion/krea2.hpp
index 0628e3c..9e8c53b 100644
--- src/model/diffusion/krea2.hpp
+++ src/model/diffusion/krea2.hpp
@@ -651,7 +651,11 @@ namespace Krea2 {
                 return tensor;
             }
             if (tensor->type != GGML_TYPE_F32 || tensor->ne[2] != 1 || tensor->ne[3] != 1) {
-                request_->execution_failed = true;
+                request_->fail("activation tensor at site " + std::to_string(site) +
+                               " has type " + std::to_string(tensor->type) +
+                               " and trailing dimensions " + std::to_string(tensor->ne[2]) +
+                               "x" + std::to_string(tensor->ne[3]) +
+                               "; expected F32 with trailing dimensions 1x1");
                 return tensor;
             }
 
@@ -671,7 +675,10 @@ namespace Krea2 {
                                                reference_start,
                                                reference_length);
                 if (selected == nullptr) {
-                    request_->execution_failed = true;
+                    request_->fail("capture " + std::to_string(capture.capture_index) +
+                                   " could not select its requested token range at site " +
+                                   std::to_string(site) + " on branch " +
+                                   std::to_string(branch_));
                     return tensor;
                 }
                 captures[capture.capture_index] = selected;
@@ -679,7 +686,8 @@ namespace Krea2 {
                     uint64_t bytes = static_cast<uint64_t>(ggml_nbytes(selected));
                     if (snapshot_device_bytes >
                         std::numeric_limits<uint64_t>::max() - bytes) {
-                        request_->execution_failed = true;
+                        request_->fail("device snapshot byte accounting overflowed at capture " +
+                                       std::to_string(capture.capture_index));
                         return tensor;
                     }
                     snapshot_device_bytes += bytes;
@@ -697,7 +705,10 @@ namespace Krea2 {
             if (request_->resident_device_bytes > request_->maximum_device_bytes ||
                 snapshot_device_bytes >
                     request_->maximum_device_bytes - request_->resident_device_bytes) {
-                request_->execution_failed = true;
+                request_->fail("activation device budget exceeded: resident=" +
+                               std::to_string(request_->resident_device_bytes) +
+                               " snapshot=" + std::to_string(snapshot_device_bytes) +
+                               " maximum=" + std::to_string(request_->maximum_device_bytes));
                 return tensor;
             }
             request_->peak_device_bytes = std::max(
@@ -719,7 +730,10 @@ namespace Krea2 {
                                              reference_start,
                                              reference_length);
                 if (before == nullptr) {
-                    request_->execution_failed = true;
+                    request_->fail("operation " + std::to_string(operation.operation_index) +
+                                   " could not select its requested token range at site " +
+                                   std::to_string(site) + " on branch " +
+                                   std::to_string(branch_));
                     return tensor;
                 }
                 ggml_tensor* source = nullptr;
@@ -733,13 +747,22 @@ namespace Krea2 {
                 }
                 if (source == nullptr || source->type != GGML_TYPE_F32 ||
                     source->ne[0] != before->ne[0]) {
-                    request_->execution_failed = true;
+                    request_->fail("operation " + std::to_string(operation.operation_index) +
+                                   " source is incompatible at site " + std::to_string(site) +
+                                   ": source_present=" + std::to_string(source != nullptr) +
+                                   " source_type=" +
+                                   std::to_string(source == nullptr ? -1 : source->type) +
+                                   " source_width=" +
+                                   std::to_string(source == nullptr ? -1 : source->ne[0]) +
+                                   " target_width=" + std::to_string(before->ne[0]));
                     return tensor;
                 }
 
                 ggml_tensor* after = transform(before, source, operation);
                 if (after == nullptr) {
-                    request_->execution_failed = true;
+                    request_->fail("operation " + std::to_string(operation.operation_index) +
+                                   " transform construction failed at site " +
+                                   std::to_string(site));
                     return tensor;
                 }
                 current = splice(current,
@@ -752,12 +775,16 @@ namespace Krea2 {
                                  reference_start,
                                  reference_length);
                 if (current == nullptr) {
-                    request_->execution_failed = true;
+                    request_->fail("operation " + std::to_string(operation.operation_index) +
+                                   " result splice failed at site " + std::to_string(site));
                     return tensor;
                 }
                 if (records_boundary(operation.boundary)) {
                     if (request_->applications >= request_->maximum_applications) {
-                        request_->execution_failed = true;
+                        request_->fail("activation application limit exceeded: applied=" +
+                                       std::to_string(request_->applications) +
+                                       " maximum=" +
+                                       std::to_string(request_->maximum_applications));
                         return tensor;
                     }
                     ++request_->applications;
@@ -1601,7 +1628,16 @@ namespace Krea2 {
             for (const auto& event : activation_events) {
                 if (event.tensor == nullptr || event.tensor->buffer == nullptr ||
                     event.tensor->type != GGML_TYPE_F32) {
-                    request.execution_failed = true;
+                    request.fail("activation event " + std::to_string(event.index) +
+                                 " kind=" + std::to_string(event.kind) +
+                                 " has an invalid tensor: tensor_present=" +
+                                 std::to_string(event.tensor != nullptr) +
+                                 " buffer_present=" +
+                                 std::to_string(event.tensor != nullptr &&
+                                                event.tensor->buffer != nullptr) +
+                                 " type=" +
+                                 std::to_string(event.tensor == nullptr ? -1 :
+                                                                  event.tensor->type));
                     continue;
                 }
                 size_t bytes = ggml_nbytes(event.tensor);
@@ -1610,7 +1646,7 @@ namespace Krea2 {
                 for (const auto& retained : before_values) {
                     if (retained_host_bytes >
                         std::numeric_limits<uint64_t>::max() - retained.second.size()) {
-                        request.execution_failed = true;
+                        request.fail("retained activation event byte accounting overflowed");
                         break;
                     }
                     retained_host_bytes += retained.second.size();
@@ -1621,7 +1657,12 @@ namespace Krea2 {
                     retained_host_bytes > request.maximum_host_bytes ||
                     event_bytes >
                         (request.maximum_host_bytes - retained_host_bytes) / copies) {
-                    request.execution_failed = true;
+                    request.fail("activation host budget exceeded at event " +
+                                 std::to_string(event.index) + ": retained=" +
+                                 std::to_string(retained_host_bytes) + " event=" +
+                                 std::to_string(event_bytes) + " copies=" +
+                                 std::to_string(copies) + " maximum=" +
+                                 std::to_string(request.maximum_host_bytes));
                     continue;
                 }
                 request.peak_host_bytes = std::max(
@@ -1632,7 +1673,10 @@ namespace Krea2 {
                 if (std::any_of(values.begin(), values.end(), [](float value) {
                         return !std::isfinite(value);
                     })) {
-                    request.execution_failed = true;
+                    request.fail("activation event " + std::to_string(event.index) +
+                                 " kind=" + std::to_string(event.kind) +
+                                 " contains a non-finite value among " +
+                                 std::to_string(values.size()) + " elements");
                     continue;
                 }
                 if (event.kind == KREA_ACTIVATION_APPLICATION_BEFORE_EVENT) {
@@ -1658,7 +1702,9 @@ namespace Krea2 {
                 }
             }
             if (!before_values.empty()) {
-                request.execution_failed = true;
+                request.fail("activation event stream ended with " +
+                             std::to_string(before_values.size()) +
+                             " unmatched before-application values");
             }
         }
     };
diff --git src/model/diffusion/model.hpp src/model/diffusion/model.hpp
index a16c3d6..6cba91b 100644
--- src/model/diffusion/model.hpp
+++ src/model/diffusion/model.hpp
@@ -177,6 +177,7 @@ struct KreaActivationRequest {
     void* callback_data                  = nullptr;
     bool callback_failed                 = false;
     bool execution_failed                = false;
+    std::string execution_failure;
     uint64_t maximum_host_bytes          = 0;
     uint64_t maximum_device_bytes        = 0;
     uint64_t maximum_applications        = 0;
@@ -184,6 +185,13 @@ struct KreaActivationRequest {
     uint64_t peak_host_bytes             = 0;
     uint64_t peak_device_bytes           = 0;
     uint64_t applications                = 0;
+
+    void fail(std::string message) {
+        execution_failed = true;
+        if (execution_failure.empty()) {
+            execution_failure = std::move(message);
+        }
+    }
 };
 
 struct KreaActivationSiteTopology {
diff --git src/stable-diffusion.cpp src/stable-diffusion.cpp
index 3d1b648..8485c8d 100644
--- src/stable-diffusion.cpp
+++ src/stable-diffusion.cpp
@@ -2,6 +2,7 @@
 #include <cmath>
 #include <cstring>
 #include <cstdlib>
+#include <exception>
 #include <limits>
 #include <new>
 #include <set>
@@ -5884,9 +5885,15 @@ int32_t sd_loom_generate_image_v1(sd_ctx_t* sd_ctx,
         return status;
     }
     if (!generated || images == nullptr || image_count != 1) {
+        LOG_ERROR("Logit Loom image generation returned generated=%d images=%p image_count=%d status=%d",
+                  generated ? 1 : 0,
+                  static_cast<void*>(images),
+                  image_count,
+                  status);
         if (images != nullptr) {
             free_sd_images(images, image_count);
         }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 
@@ -6136,10 +6143,24 @@ int32_t sd_loom_generate_image_v2(sd_ctx_t* sd_ctx,
         }
         sd_ctx->sd->apply_loras(nullptr, 0);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         status = SD_LOOM_STATUS_NATIVE_ERROR;
         try {
             sd_ctx->sd->apply_loras(nullptr, 0);
         } catch (...) {
+            try {
+                throw;
+            } catch (const std::exception& error) {
+                LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+            } catch (...) {
+                LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+            }
         }
     }
     sd_loom_reset_callbacks(sd_ctx);
@@ -6155,6 +6176,7 @@ int32_t sd_loom_generate_image_v2(sd_ctx_t* sd_ctx,
         if (images != nullptr) {
             free_sd_images(images, image_count);
         }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 
@@ -6197,17 +6219,20 @@ int32_t sd_loom_vae_encode_v2(sd_ctx_t* sd_ctx,
         auto pixels = sd_image_to_tensor(sd_loom_native_image_v2(*image));
         auto latent = sd_ctx->sd->encode_first_stage(pixels);
         if (latent.empty() || latent.shape().empty() || latent.shape().size() > 8) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         auto latent_elements = latent.numel();
         if (latent_elements <= 0 ||
             static_cast<uint64_t>(latent_elements) > 16 * 1024 * 1024 ||
             static_cast<uint64_t>(latent_elements) > SIZE_MAX / sizeof(float)) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         auto* result = static_cast<sd_loom_owned_tensor_v2_t*>(
             calloc(1, sizeof(sd_loom_owned_tensor_v2_t)));
         if (result == nullptr) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         result->abi_version   = SD_LOOM_IMAGE_ABI_VERSION;
@@ -6217,6 +6242,7 @@ int32_t sd_loom_vae_encode_v2(sd_ctx_t* sd_ctx,
         result->shape         = static_cast<int64_t*>(malloc(result->rank * sizeof(int64_t)));
         if (result->data == nullptr || result->shape == nullptr) {
             sd_loom_free_owned_tensor_v2(result);
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         std::memcpy(result->data, latent.data(), result->element_count * sizeof(float));
@@ -6224,6 +6250,14 @@ int32_t sd_loom_vae_encode_v2(sd_ctx_t* sd_ctx,
         *tensor_out = result;
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6273,20 +6307,31 @@ int32_t sd_loom_vae_decode_v2(sd_ctx_t* sd_ctx,
         sd::Tensor<float> latent(std::move(shape), std::move(data));
         auto decoded = sd_ctx->sd->decode_first_stage(latent);
         if (decoded.empty()) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         auto* result = static_cast<sd_image_t*>(calloc(1, sizeof(sd_image_t)));
         if (result == nullptr) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         result[0] = tensor_to_sd_image(decoded);
         if (result[0].data == nullptr) {
             free(result);
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         *image_out = result;
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6305,10 +6350,19 @@ int32_t sd_loom_clear_session_v2(sd_ctx_t* sd_ctx) {
             (sd_ctx->sd->version == VERSION_KREA2 &&
              sd_ctx->sd->diffusion_model != nullptr &&
              !sd_ctx->sd->diffusion_model->krea_activation_clear())) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6413,6 +6467,14 @@ static int32_t sd_loom_add_value_v3(sd_loom_program_v3_t* program,
     try {
         program->values.push_back(std::move(value));
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     program->live_bytes += bytes;
@@ -6491,12 +6553,21 @@ int32_t sd_loom_program_begin_v3(sd_ctx_t* sd_ctx,
     try {
         auto* program = new (std::nothrow) sd_loom_program_v3_t();
         if (program == nullptr) {
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         try {
             program->values.reserve(maximum_values);
         } catch (...) {
+            try {
+                throw;
+            } catch (const std::exception& error) {
+                LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+            } catch (...) {
+                LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+            }
             delete program;
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         sd_ctx->sd->loom_program_generation += 1;
@@ -6508,6 +6579,14 @@ int32_t sd_loom_program_begin_v3(sd_ctx_t* sd_ctx,
         *program_out = program;
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6525,6 +6604,14 @@ int32_t sd_loom_program_import_bytes_v3(sd_loom_program_v3_t* program,
         value.bytes.assign(bytes, bytes + byte_count);
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6545,6 +6632,14 @@ int32_t sd_loom_program_import_image_v3(sd_loom_program_v3_t* program,
         value.channels = image->channels;
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6565,6 +6660,14 @@ int32_t sd_loom_program_import_tensor_v3(sd_loom_program_v3_t* program,
         value.shape.assign(tensor->shape, tensor->shape + tensor->rank);
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6583,6 +6686,14 @@ int32_t sd_loom_program_import_lora_v3(sd_loom_program_v3_t* program,
         value.is_high_noise = is_high_noise;
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6631,6 +6742,14 @@ int32_t sd_loom_program_mask_blend_v3(sd_loom_program_v3_t* program,
         }
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6720,6 +6839,7 @@ static int32_t sd_loom_add_output_image_v3(
     }
     size_t pixels = static_cast<size_t>(image->width) * image->height;
     if (pixels > SIZE_MAX / output_channels) {
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     try {
@@ -6764,6 +6884,7 @@ static int32_t sd_loom_add_output_image_v3(
             if (encoded != nullptr) {
                 STBIW_FREE(encoded);
             }
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         if (static_cast<uint64_t>(encoded_bytes) > maximum_output_bytes) {
@@ -6773,12 +6894,27 @@ static int32_t sd_loom_add_output_image_v3(
         try {
             value.bytes.assign(encoded, encoded + encoded_bytes);
         } catch (...) {
+            try {
+                throw;
+            } catch (const std::exception& error) {
+                LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+            } catch (...) {
+                LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+            }
             STBIW_FREE(encoded);
             throw;
         }
         STBIW_FREE(encoded);
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6799,6 +6935,7 @@ int32_t sd_loom_program_vae_encode_v3(sd_loom_program_v3_t* program,
         return status;
     }
     if (encoded == nullptr) {
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     try {
@@ -6809,7 +6946,15 @@ int32_t sd_loom_program_vae_encode_v3(sd_loom_program_v3_t* program,
         sd_loom_free_tensor_v2(encoded);
         return sd_loom_add_value_v3(program, std::move(value), value_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         sd_loom_free_tensor_v2(encoded);
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -6856,6 +7001,7 @@ int32_t sd_loom_program_vae_decode_v3(sd_loom_program_v3_t* program,
         if (image != nullptr) {
             free_sd_images(image, 1);
         }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     int32_t output_status = sd_loom_add_output_image_v3(program,
@@ -6955,6 +7101,13 @@ static bool sd_loom_text_value_v3(const sd_loom_program_v3_t* program,
                          value->bytes.size());
         return true;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         return false;
     }
 }
@@ -7086,6 +7239,7 @@ static int32_t sd_loom_add_step_state_v3(sd_loom_program_step_state_v3_t* state,
         step->shape == nullptr ||
         step->rank == 0 ||
         step->rank > 8) {
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     sd_loom_value_v3_t value;
@@ -7183,6 +7337,13 @@ static int32_t sd_loom_program_step_callback_v3(const sd_loom_step_v1_t* step,
         }
         return SD_LOOM_CALLBACK_CONTINUE;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         state->failure_status = SD_LOOM_STATUS_NATIVE_ERROR;
         return SD_LOOM_CALLBACK_ERROR;
     }
@@ -7328,6 +7489,14 @@ int32_t sd_loom_program_generate_image_v3(
             expected_lora_boundaries += params->loras[lora].point_count - 1;
         }
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 
@@ -7412,6 +7581,18 @@ int32_t sd_loom_program_generate_image_v3(
                                                      params->maximum_output_bytes,
                                                      &primary);
         } else if (status == SD_LOOM_STATUS_OK) {
+            LOG_ERROR("Logit Loom program output invariant failed: snapshots=%zu/%zu LoRA boundaries=%zu/%zu image=%p data=%p geometry=%ux%ux%u expected=%dx%d",
+                      callback_state.snapshot_cursor,
+                      params->snapshot_count,
+                      callback_state.applied_lora_boundaries,
+                      expected_lora_boundaries,
+                      static_cast<void*>(image),
+                      image == nullptr ? nullptr : static_cast<void*>(image->data),
+                      image == nullptr ? 0 : image->width,
+                      image == nullptr ? 0 : image->height,
+                      image == nullptr ? 0 : image->channel,
+                      params->width,
+                      params->height);
             status = SD_LOOM_STATUS_NATIVE_ERROR;
         }
 
@@ -7422,6 +7603,7 @@ int32_t sd_loom_program_generate_image_v3(
             if (!sd_loom_rollback_generation_v3(program,
                                                  &callback_state,
                                                  &primary)) {
+                LOG_ERROR("Logit Loom native error branch in %s", __func__);
                 return SD_LOOM_STATUS_NATIVE_ERROR;
             }
             return status;
@@ -7435,6 +7617,14 @@ int32_t sd_loom_program_generate_image_v3(
         }
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7580,7 +7770,15 @@ int32_t sd_loom_program_generate_image_v4(
                                                   snapshot_capacity,
                                                   result_out);
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         program->context->sd->loom_model_block_controls.clear();
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7770,6 +7968,7 @@ int32_t sd_loom_program_generate_image_v5(
                 control.selected_transitions.size() !=
                     transition_words_per_operator) {
                 program->context->sd->loom_model_block_controls.clear();
+                LOG_ERROR("Logit Loom native error branch in %s", __func__);
                 return SD_LOOM_STATUS_NATIVE_ERROR;
             }
             application_results[control.application_index] = {
@@ -7798,7 +7997,15 @@ int32_t sd_loom_program_generate_image_v5(
             program->context->sd->loom_model_block_controls.empty() ? 1 : 0;
         return status;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         program->context->sd->loom_model_block_controls.clear();
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7871,6 +8078,14 @@ int32_t sd_loom_krea_topology_v6(
         }
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7904,6 +8119,14 @@ int32_t sd_loom_krea_import_input_v6(
         sd_loom_krea_description_v6(description, description_out);
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7930,6 +8153,14 @@ int32_t sd_loom_krea_describe_input_v6(
         sd_loom_krea_description_v6(description, description_out);
         return SD_LOOM_STATUS_OK;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7948,6 +8179,14 @@ int32_t sd_loom_krea_release_input_v6(
                    ? SD_LOOM_STATUS_OK
                    : SD_LOOM_STATUS_INVALID_ARGUMENT;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -7963,6 +8202,14 @@ int32_t sd_loom_krea_clear_inputs_v6(sd_ctx_t* sd_ctx) {
                    ? SD_LOOM_STATUS_OK
                    : SD_LOOM_STATUS_UNSUPPORTED;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -8433,6 +8680,13 @@ int32_t sd_loom_program_generate_image_v6(
         result_out->peak_host_bytes = request.peak_host_bytes;
         result_out->peak_device_bytes = request.peak_device_bytes;
         if (!cleaned || request.execution_failed) {
+            LOG_ERROR("Krea activation cleanup/execution failed: cleaned=%d execution_failed=%d detail=%s",
+                      cleaned ? 1 : 0,
+                      request.execution_failed ? 1 : 0,
+                      request.execution_failure.empty()
+                          ? "no execution failure was recorded"
+                          : request.execution_failure.c_str());
+            LOG_ERROR("Logit Loom native error branch in %s", __func__);
             return SD_LOOM_STATUS_NATIVE_ERROR;
         }
         if (request.callback_failed) {
@@ -8440,10 +8694,18 @@ int32_t sd_loom_program_generate_image_v6(
         }
         return status;
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         program->context->sd->loom_krea_activation = nullptr;
         if (program->context->sd->diffusion_model != nullptr) {
             program->context->sd->diffusion_model->krea_activation_finish_request();
         }
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
 }
@@ -8498,6 +8760,7 @@ int32_t sd_loom_program_read_v3(const sd_loom_program_v3_t* program,
     }
     uint64_t bytes = value->data_bytes();
     if (bytes == 0 || bytes > SIZE_MAX) {
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     int32_t status = callback(sd_loom_value_bytes_v3(*value),
@@ -8537,6 +8800,7 @@ int32_t sd_loom_program_release_v3(sd_loom_program_v3_t* program,
     }
     uint64_t bytes = value->resident_bytes();
     if (bytes > program->live_bytes) {
+        LOG_ERROR("Logit Loom native error branch in %s", __func__);
         return SD_LOOM_STATUS_NATIVE_ERROR;
     }
     program->live_bytes -= bytes;
@@ -8569,6 +8833,13 @@ int32_t sd_loom_program_finish_v3(sd_loom_program_v3_t* program,
             status = sd_loom_clear_session_v2(context);
         }
     } catch (...) {
+        try {
+            throw;
+        } catch (const std::exception& error) {
+            LOG_ERROR("Logit Loom native exception in %s: %s", __func__, error.what());
+        } catch (...) {
+            LOG_ERROR("Logit Loom native exception in %s: non-standard exception", __func__);
+        }
         status = SD_LOOM_STATUS_NATIVE_ERROR;
     }
     if (status == SD_LOOM_STATUS_OK) {
